import React from "react"; //obj from module
import ReactDOM from "react-dom";

const element = <h1>welcome here!</h1>;
ReactDOM.render(element, document.getElementById("root")); //elt to render
//console.log(element);
//React.createElement
